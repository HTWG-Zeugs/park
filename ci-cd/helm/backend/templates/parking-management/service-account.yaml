apiVersion: v1
kind: ServiceAccount
metadata:
  name: parking-management-sa
  namespace: {{ $.Release.Namespace }}
  annotations:
    iam.gke.io/gcp-service-account: "{{ .Values.tenant_id }}-park@{{ .Values.gcp.project_id }}.iam.gserviceaccount.com"