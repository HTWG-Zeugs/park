apiVersion: v1
kind: ServiceAccount
metadata:
  name: parking-management-sa
  namespace: {{ $.Release.Namespace }}
  annotations:
    iam.gke.io/gcp-service-account: "{{ $.Release.Namespace }}-parking-management-sa@{{ .Values.gcp.project_id }}.iam.gserviceaccount.com"