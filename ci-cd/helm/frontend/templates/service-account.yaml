apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend-sa
  namespace: {{ $.Release.Namespace }}
  annotations:
    iam.gke.io/gcp-service-account: "{{ $.Release.Namespace }}-frontend-sa@{{ .Values.gcp.project_id }}.iam.gserviceaccount.com"